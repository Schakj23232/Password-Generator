# Password-Generator

# Features
  * You can choose with the `input` function how long you want the password to be

Very simple, just uses a `while` loop with the `len` function to see how many letters long you want the password to be, Then adds a letter and then symbol and repeats till it's as long as it needs to be.
